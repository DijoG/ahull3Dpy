name: Build wheels with cibuildwheel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-latest, macos-13]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python (for setup)
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install CGAL (macOS)
        if: runner.os == 'macOS'
        run: |
          brew update || true
          brew install cgal eigen

      - name: Install CGAL (Windows)
        if: runner.os == 'Windows'
        run: |
          git clone https://github.com/microsoft/vcpkg.git
          cd vcpkg
          .\bootstrap-vcpkg.bat
          .\vcpkg install cgal:x64-windows

      - name: Build wheels
        uses: pypa/cibuildwheel@v2.22.0
        env:
          CIBW_BUILD: "cp39-* cp310-* cp311-* cp312-*"
          CIBW_SKIP: "*-win32 *-manylinux_i686 *-musllinux*"
          CIBW_ARCHS_LINUX: "x86_64"
          CIBW_ARCHS_WINDOWS: "AMD64"
          CIBW_ARCHS_MACOS: "x86_64 arm64"
          
          # Windows: Pass CGAL path
          CIBW_ENVIRONMENT_WINDOWS: >-
            CGAL_INCLUDE_PATH=${{ github.workspace }}\vcpkg\installed\x64-windows\include
          
          # macOS: CGAL is in Homebrew path
          CIBW_ENVIRONMENT_MACOS: >-
            CGAL_INCLUDE_PATH=/opt/homebrew/include
          
          # Linux: Manylinux container needs CGAL installed
          CIBW_BEFORE_ALL_LINUX: |
            yum install -y epel-release
            yum install -y eigen3-devel
            yum install -y cgal-devel || echo "CGAL not in yum, installing from source..."
            if ! yum list installed cgal-devel 2>/dev/null; then
              yum install -y cmake make wget gcc-c++
              cd /tmp
              wget https://github.com/CGAL/cgal/releases/download/v6.0/CGAL-6.0.tar.gz
              tar -xzf CGAL-6.0.tar.gz
              cd CGAL-6.0
              cmake -B build -DCMAKE_INSTALL_PREFIX=/usr/local
              cmake --build build --target install -j4
              cd / && rm -rf /tmp/CGAL-6.0*
            fi
          
          CIBW_BEFORE_BUILD: >-
            pip install pybind11 numpy scipy

      - uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}
          path: ./wheelhouse/*.whl